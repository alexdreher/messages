%h1 Listing messages

%table
  %tr
    %th Sender
    %th Recipient
    %th Subject
    %th Body
    %th
    %th

  - @messages.each do |message|
    %tr
      %td= message.sender_id
      %td= message.recipient_id
      %td= message.subject
      %td= message.body
      %td= link_to 'Show', message
      %td= link_to 'Destroy', message, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Message', new_message_path
